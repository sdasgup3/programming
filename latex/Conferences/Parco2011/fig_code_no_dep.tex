{\tt
\begin{program}{10}
  \FL\ \ldots
  \UNL{0} p = list;
  \UNL{0} \WHILE (p->next != NULL) \{
  \NL{1}     \ldots = p->next->num;
  \NL{1}     p->next->next->num = \ldots;
  \NL{1}     p = p->next->next;
  \UNL{0} \}
  \UNL{0} \ldots
\end{program}
}
